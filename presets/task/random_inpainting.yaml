operator:
  name: inpainting
  mask_path: presets/masks/random_mask.png

noise: 
  name: gaussian
  sigma: 0.05

pixel:
  annealing:
    steps: 50
    sigma_min: 0.05
    sigma_max: 80.0
    rho: 7

  sampler:
    ts: null
    ema_sigma: 0.5
    ema_decay: 0.1

  beta: 1e-6